@model Doctor

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<h3>Пациенти:</h3>
<ul>
    @foreach (var p in Model.Patients)
    {
        <li>@Html.ActionLink(p.FullName, "Details", "Patient", new { id = p.Id }, null)</li>
    }
</ul>

@Html.ActionLink("Додади пациент", "AddPatient", new { id = Model.Id }, new { @class = "btn btn-success" })


<div>
    <h4>Doctor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Hospital.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Hospital.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.FullName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FullName)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
